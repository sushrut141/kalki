name: Build And Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Pull LFS Files
        run: git lfs pull

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            ca-certificates \
            cmake \
            git-lfs \
            pkg-config \
            ninja-build \
            protobuf-compiler \
            libprotobuf-dev \
            libgrpc++-dev \
            protobuf-compiler-grpc \
            libsqlite3-dev \
            libcurl4-openssl-dev \
            libzstd-dev \
            libgtest-dev \
            libopenblas-dev \
            liblapack-dev

      - name: Build Abseil
        run: |
          git clone --depth=1 https://github.com/abseil/abseil-cpp.git /tmp/abseil-cpp
          cmake -S /tmp/abseil-cpp -B /tmp/abseil-cpp/build \
            -DABSL_BUILD_TESTING=OFF \
            -DBUILD_SHARED_LIBS=ON \
            -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
            -DCMAKE_BUILD_TYPE=Release
          cmake --build /tmp/abseil-cpp/build -j"$(nproc)"
          sudo cmake --install /tmp/abseil-cpp/build --prefix /usr/local

      - name: Build FAISS
        run: |
          git clone --depth=1 https://github.com/facebookresearch/faiss.git /tmp/faiss
          cmake -S /tmp/faiss -B /tmp/faiss/build \
            -DBUILD_TESTING=OFF \
            -DFAISS_ENABLE_GPU=OFF \
            -DFAISS_ENABLE_PYTHON=OFF \
            -DBUILD_SHARED_LIBS=ON
          cmake --build /tmp/faiss/build -j"$(nproc)"
          sudo cmake --install /tmp/faiss/build --prefix /usr/local

      - name: Build llama.cpp
        run: |
          git clone --depth=1 https://github.com/ggml-org/llama.cpp.git /tmp/llama.cpp
          cmake -S /tmp/llama.cpp -B /tmp/llama.cpp/build -DBUILD_SHARED_LIBS=ON
          cmake --build /tmp/llama.cpp/build -j"$(nproc)"
          sudo cmake --install /tmp/llama.cpp/build --prefix /usr/local
          sudo ldconfig

      - name: Configure
        run: cmake -S . -B build -GNinja -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH=/usr/local

      - name: Build
        run: cmake --build build -j"$(nproc)"

      - name: Test
        env:
          LD_LIBRARY_PATH: /usr/local/lib:/usr/local/lib64:${{ env.LD_LIBRARY_PATH }}
        run: ctest --test-dir build --output-on-failure
